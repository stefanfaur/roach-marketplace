[project]
name = "mariadb-mcp-tests"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-timeout>=2.0",
    "pexpect>=4.9",
]

[tool.pytest.ini_options]
timeout = 120
asyncio_mode = "auto"
