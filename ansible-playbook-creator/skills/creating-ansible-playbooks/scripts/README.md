# Scripts

Utility scripts for Ansible playbook validation, security scanning, and testing.

- `secure_playbook.py` — Security vulnerability scanner for playbooks
- `validate_playbook.py` — Syntax and structure validator
- `test_playbook.sh` — Container-based playbook testing
